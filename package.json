{
  "name": "t3-code-mcp-prime",
  "version": "0.2.0",
  "description": "Prime-inspired bounded T3 conversation retrieval, exact-request follow-ups, and JSON scripting.",
  "type": "module",
  "bin": {
    "t3-code-mcp-prime": "dist/server.js",
    "t3-mcp-prime": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "smoke": "node scripts/prime-smoke.mjs",
    "test": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests -p '*_test.py'"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  },
  "private": true
}
